# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-ssr/
build/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db

# Env & secrets
.env
.env.*
!.env.example

# Test / coverage
coverage/
.nyc_output/

# Tauri signing keys (NEVER commit)
*.key
*.pem

# App runtime assets — APK TikTok + build MEmu (nặng, không phải mã nguồn)
appTiktok/

# Bản build cài đặt (artifact, không commit)
release/
